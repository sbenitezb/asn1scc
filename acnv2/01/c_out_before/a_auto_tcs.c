/*
Code automatically generated by asn1scc tool
*/

#include <string.h>
#include <math.h>
#include <limits.h>
#include <stdio.h>
#include "asn1crt.h"
#include "a_auto_tcs.h"


flag RGB_t_ACN_enc_dec(const RGB_t* pVal, int* pErrCode, const char* filename)
{
    static RGB_t decodedPDU;
    static byte encBuff[RGB_t_REQUIRED_BYTES_FOR_ACN_ENCODING + 1]; /* +1 for zerosized types */
    BitStream bitStrm;
    flag ret = TRUE;

    BitStream_Init(&bitStrm, encBuff, RGB_t_REQUIRED_BYTES_FOR_ACN_ENCODING);
    // Encode value
    ret = RGB_t_ACN_Encode(pVal, &bitStrm, pErrCode, TRUE);
    *pErrCode = (ret && *pErrCode ==0) ? 0 : 1;
    if (ret) {
        BitStream_AttachBuffer(&bitStrm, encBuff, RGB_t_REQUIRED_BYTES_FOR_ACN_ENCODING);
        // Decode value
        ret = RGB_t_ACN_Decode(&decodedPDU, &bitStrm, pErrCode);
        *pErrCode = (ret && *pErrCode ==0) ? 0 : 2;
        if (ret) {
            // validate decoded data
            ret = RGB_t_IsConstraintValid(&decodedPDU, pErrCode);
            *pErrCode = (ret && *pErrCode ==0) ? 0 : 3;
            if (ret) {
                ret = RGB_t_Equal(pVal, &decodedPDU);
                *pErrCode = ret ? 0 : 4;
                if (ret) {
                    char buf[1024];
                    strcpy(buf, filename);
                    FILE* fp = fopen(strcat(buf,".dat"), "wb");
                    fwrite(bitStrm.buf, 1, bitStrm.currentBit > 0 ? bitStrm.currentByte + 1 : bitStrm.currentByte, fp);
                    fclose(fp);
                
                }
            
            }
        
        }
    
    }

    return ret;
}
flag PDU_ACN_enc_dec(const PDU* pVal, int* pErrCode, const char* filename)
{
    static PDU decodedPDU;
    static byte encBuff[PDU_REQUIRED_BYTES_FOR_ACN_ENCODING + 1]; /* +1 for zerosized types */
    BitStream bitStrm;
    flag ret = TRUE;

    BitStream_Init(&bitStrm, encBuff, PDU_REQUIRED_BYTES_FOR_ACN_ENCODING);
    // Encode value
    ret = PDU_ACN_Encode(pVal, &bitStrm, pErrCode, TRUE);
    *pErrCode = (ret && *pErrCode ==0) ? 0 : 1;
    if (ret) {
        BitStream_AttachBuffer(&bitStrm, encBuff, PDU_REQUIRED_BYTES_FOR_ACN_ENCODING);
        // Decode value
        ret = PDU_ACN_Decode(&decodedPDU, &bitStrm, pErrCode);
        *pErrCode = (ret && *pErrCode ==0) ? 0 : 2;
        if (ret) {
            // validate decoded data
            ret = PDU_IsConstraintValid(&decodedPDU, pErrCode);
            *pErrCode = (ret && *pErrCode ==0) ? 0 : 3;
            if (ret) {
                ret = PDU_Equal(pVal, &decodedPDU);
                *pErrCode = ret ? 0 : 4;
                if (ret) {
                    char buf[1024];
                    strcpy(buf, filename);
                    FILE* fp = fopen(strcat(buf,".dat"), "wb");
                    fwrite(bitStrm.buf, 1, bitStrm.currentBit > 0 ? bitStrm.currentByte + 1 : bitStrm.currentByte, fp);
                    fclose(fp);
                
                }
            
            }
        
        }
    
    }

    return ret;
}
